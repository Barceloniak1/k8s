apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jenkins-persistent-storage
  namespace: jenkins
  labels:
    app: jenkins
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 6Gi
